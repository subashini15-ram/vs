git and github git log
sample change to track file 
Git Configuration & Setupgit init – Creates a new local repository.git clone <url> – Downloads an existing repository from GitHub.git config --global user.name "Name" – Sets your commit username.git config --global user.email "email" – Sets your commit email. 
if you update or change in document. to add it to git by < git addd . >  then mark changes <git commit -m "updated 2nd para">